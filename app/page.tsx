'use client'; // Ensure the component is client-side

import IntroPage from "../app/components/IntroPage"; // Correct import path

export default function Home() {
  return <IntroPage />; // Render the IntroPage component
}
